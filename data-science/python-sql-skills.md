# Data Science & Database Copilot Skills
1. **Pandas Manipulation**: `# group by department and calculate mean salary` -> Outputs `df.groupby('dept')['salary'].mean()`.
2. **Matplotlib/Seaborn Viz**: `# plot a scatter chart of age vs income with a regression line` -> Full plotting code.
3. **Natural Language to SQL**: `-- Select top 5 customers by total order value in 2023` -> Complex JOINs and aggregations generated.
4. **Jupyter Notebook Integration**: Copilot predicts next cells and explains complex mathematical functions.
