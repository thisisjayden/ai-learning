# Frontend Copilot Skills (React/Vue/TS)
1. **Component Generation**: Type `// React component for a user profile card with Tailwind CSS` -> Gets full TSX + styling.
2. **Regex/Form Validation**: `// Email validation regex` -> instant JS regex.
3. **Jest/Vitest Automation**: Highlight component -> `/tests` -> Generates DOM rendering and interaction tests.
4. **Mock Data Generation**: `// Array of 10 dummy user objects with id, name, avatar` -> Instantly populated JSON/JS objects.
